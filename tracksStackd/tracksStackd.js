// resortproxy
// tracks proxy
var ResortProxy = (function () {

    // base address from config somewhere
    function ResortProxy() {

    }

    var getResort = function (id) {
        //ajax request
    };

    var addResort = function (resort) {

    };

    var deleteResort = function (id) {

    };

    return ResortProxy;
})();

// jquery deferred wrappers for resortservice